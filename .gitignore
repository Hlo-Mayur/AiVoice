# ## General Logs
# Logs generated during development or runtime
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# ## Dependencies
# These can be reinstalled using `npm install` or `yarn install`
node_modules/
pnpm-lock.yaml

# ## Build / Production Folders
# These are generated by the build process
dist/
dist-ssr/
build/
out/
coverage/

# ## Local Environment Configuration
# Never commit secrets, API keys, or credentials
.env
.env.local
.env.*.local
*.local

# ## Editor & Operating System Specific Files
# These are user-specific and should not be shared
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
.idea/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?